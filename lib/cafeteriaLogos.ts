// Real cafeteria logos, keyed by name — used wherever a cafeteria's stock/
// placeholder photo should instead show its actual branding. Falls back to
// whatever the caller already does (emoji chip, generic photo) when a
// cafeteria has no entry here.
export const CAFETERIA_LOGOS: { [key: string]: string } = {
  'LETHAFI': '/logos/lethafi.png',
}
